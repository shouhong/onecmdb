Desktop configurations for user/roles is placed here.
The algorithm for fetching desktop conf is as follows:
1) Use roles/"Role"/"username"/desktopView.xml if found.
2) Use roles/"Role"/desktopView.xml if found
3) Use defaultView.xml.